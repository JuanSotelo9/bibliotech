db.url=${DB_URL:jdbc:postgresql://db:5432/LibreriaDB}
db.user=${DB_USER:postgres}
db.password=${DB_PASSWORD:changeme}
db.maxPoolSize=${DB_MAX_POOL_SIZE:10}
db.minIdle=${DB_MIN_IDLE:2}
db.connectionTimeout=${DB_CONNECTION_TIMEOUT:30000}

jwt.secret=${JWT_SECRET:changeme}

cors.allowedOrigins=${CORS_ALLOWED_ORIGINS:http://localhost:3000}
